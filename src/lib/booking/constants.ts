/** Message shown when email is already registered. Shared by client (form) and server (API, createCheckout). */
export const EMAIL_ALREADY_REGISTERED_MESSAGE =
	'An account with this email is already registered.';
